#include <stdio.h>
int main(){
   double a, b, c, d;
   printf("Podaj 4 liczby rzeczywiste odziellone spacjami: ");
   int zwrot= scanf("%lf%lf%lf%lf", &a, &b, &c, &d);
   printf("Wczytano: %f %f %f %f", a, b, c, d);
   printf("\nPoprawnie wczytano %d wartosci.",zwrot);

   if( a != 0 && b!=0 )
// oblicz średnią harmoniczną z podanych liczb jeśli to jest moliwe
}
